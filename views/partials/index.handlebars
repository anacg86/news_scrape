  <!--Aqui va la info del database-->
    <div class="card border-dark mb-3" style="max-width: 20rem;">
        <div class="card-header">
            <h4 class="card-title">{{{newsTitle}}</h4>
        </div>
        <div class="card-body">
            <p class="card-text">{{{newsLink}}}</p>
        </div>
        <button type="button" class="btn btn-outline-primary">SAVE ARTICLE</button>
    </div>